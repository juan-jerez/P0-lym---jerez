


def analizarTokens(tokens)->bool:
    if token[0] == "robot_r":
        syntax = True
    while syntax == True:
        for token in tokens:
            bloque= [] 

def analizarBloqueVars(tokens)->bool:
    pass

